
#include <stdio.h>
#include <stdlib.h>
// read and allocated memory for 5 variables using malloc
/*
int main() {
    int *ptr = (int *)malloc(5 * sizeof(int));
    if (ptr == NULL) {
        printf("Memory allocation failed\n");
        return 1;
    }
    printf("Enter 5 integers:\n");
    for (int i = 0; i < 5; i++) {
        scanf("%d", &ptr[i]);
    }
    printf("You entered:\n");
    for (int i = 0; i < 5; i++) {
        printf("%d ", ptr[i]);
    }
    free(ptr);
    return 0;
}
*/
.